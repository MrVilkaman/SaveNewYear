package donnu.zolotarev.savenewyear.Utils;

import org.andengine.engine.camera.Camera;

public class ParallaxLayerControlled extends ParallaxLayer {

    public ParallaxLayerControlled(Camera camera, boolean mIsScrollable) {
        super(camera, mIsScrollable);
    }
}
